# Chrome Extension Packages

This directory now contains two separate Chrome Extension MVP packages.

- `prompt-generator`: local prompt builder
- `prompt-translator`: local prompt translator/porter

## Suggested publishing order

1. Publish `prompt-generator` first
2. Collect feedback
3. Add backend and paid plan
4. Publish a stronger `prompt-translator` once real API translation is ready
